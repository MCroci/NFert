% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nfert_quefts_bridge.R
\name{nfert_to_quefts_soil}
\alias{nfert_to_quefts_soil}
\title{Translate NFert inputs into QUEFTS soil parameters}
\usage{
nfert_to_quefts_soil(
  N_supply,
  P_supply = 10,
  K_supply = 60,
  N_recovery = 0.55,
  P_recovery = 0.1,
  K_recovery = 0.5
)
}
\arguments{
\item{N_supply}{Numeric. Plant-available N supply (kg N/ha) over the season.
Typically the \code{B} component returned by \code{\link[=N_balance]{N_balance()}}
(sum of b1 readily available N and b2 SOM mineralisation).}

\item{P_supply}{Numeric. Plant-available P supply (kg P/ha). If unknown,
defaults to 10 (representative for Italian arable soils with Olsen P
in the 15--25 mg/kg range).}

\item{K_supply}{Numeric. Plant-available K supply (kg K/ha). Default 60.}

\item{N_recovery}{Numeric. Apparent N recovery efficiency (fraction).
Default 0.55, typical for cereals on medium-textured soils.}

\item{P_recovery}{Numeric. Apparent P recovery efficiency. Default 0.10.}

\item{K_recovery}{Numeric. Apparent K recovery efficiency. Default 0.50.}
}
\value{
A named list ready to be passed to \code{\link[Rquefts:quefts]{Rquefts::quefts()}} as the
\code{soil} argument.
}
\description{
Builds the soil parameter list expected by \code{\link[Rquefts:quefts]{Rquefts::quefts()}} from the
soil logic already reflected in \code{\link[=N_balance]{N_balance()}}. The conversion is
deterministic and does not require additional measurements.
}
\details{
QUEFTS uses native nutrient supplies and recovery fractions of applied
fertiliser to build a per-cycle balance. Default recovery values follow
QUEFTS applications for European cereal systems (Janssen et al., 1990;
Sattari et al., 2014).

The \code{UptakeAdjust} matrix matches the default in \code{\link[Rquefts:quefts_soil]{Rquefts::quefts_soil()}}.
}
\references{
Janssen, B.H. et al. (1990). Geoderma 46, 299-318.
Sattari, S.Z. et al. (2014). Eur. J. Agron. 60, 79-90.
}
