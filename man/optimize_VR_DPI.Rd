% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_VR_DPI.R
\name{optimize_VR_DPI}
\alias{optimize_VR_DPI}
\title{Compliance-aware variable-rate nitrogen prescription}
\usage{
optimize_VR_DPI(
  field_inputs,
  NNI_raster,
  crop_params = NULL,
  fertilizers = NULL,
  prices = list(p_y = 280),
  zvn = TRUE,
  max_iter = 200L,
  tol = 0.01
)
}
\arguments{
\item{field_inputs}{Named list of inputs accepted by \code{\link[=N_balance]{N_balance()}} (e.g.
\code{crop}, \code{ccp}, \code{expected_yield_tons_ha}, \code{sand}, \code{clay}, \dots). Extra
elements are ignored.}

\item{NNI_raster}{A \code{terra::SpatRaster} of NNI values for the field. Cells
with \code{NA} are excluded from the prescription.}

\item{crop_params}{Either \code{NULL} (use \code{\link[=nfert_to_quefts_crop]{nfert_to_quefts_crop()}} on
\code{field_inputs$crop}), a character string (same lookup), or a named list of
QUEFTS crop parameters passed straight to \code{\link[Rquefts:quefts]{Rquefts::quefts()}}.}

\item{fertilizers}{\code{data.frame} with columns \code{name}, \code{N}, \code{P}, \code{K}, and
\code{price_kg} (EUR per kg product). Defaults to \code{fertilizers_IT.csv} in
\code{extdata}, or falls back to \code{\link[Rquefts:fertilizer]{Rquefts::fertilizers()}} with a synthetic
\code{price_kg} if needed for \code{\link[Rquefts:fertApp]{Rquefts::fertApp()}}.}

\item{prices}{Named list with \code{p_y} = crop price (EUR/t dry matter). Used as
\code{dm_crop_value} in \code{\link[Rquefts:optApp]{Rquefts::optApp()}} after conversion to EUR/kg.}

\item{zvn}{Logical. If \code{TRUE}, apply a 170 kg N/ha cap on the \strong{field-mean}
applied dose (nitrate-vulnerable zone style territorial cap).}

\item{max_iter}{Integer. Maximum iterations for mass-balance projection.}

\item{tol}{Numeric. Convergence tolerance on mean N (kg N/ha).}
}
\value{
A list with:
\describe{
\item{N_prescription}{\code{SpatRaster} of N rate (kg N/ha)}
\item{P_prescription}{\code{SpatRaster} of P2O5 rate (kg/ha)}
\item{K_prescription}{\code{SpatRaster} of K2O rate (kg/ha)}
\item{fertilizer_mix}{\code{data.frame} from \code{\link[Rquefts:fertApp]{Rquefts::fertApp()}} (may have
one row per cell, depending on Rquefts version), or empty \code{data.frame} on
failure}
\item{economics}{list: \code{mean_dose}, \code{p_y}, optional \code{note}}
\item{compliance}{list: DPI cap, MAS, ZVN flags, mass-balance check}
}
}
\description{
Generates a spatially explicit nitrogen prescription that combines the
regulatory cap from the Italian DPI nutrient balance (computed by
\code{\link[=N_balance]{N_balance()}}) with the local economic optimum nitrogen rate (EONR) computed
by the QUEFTS crop model via \strong{Rquefts}, redistributed across the field
according to Sentinel-2 nitrogen status (NNI) under a mass-balance constraint.
}
\details{
Four layers:
\enumerate{
\item \strong{Compliance:} \code{N_balance()} DPI dose; cap with \code{\link[=get_MAS]{get_MAS()}} and
optional ZVN field-mean 170 kg N/ha.
\item \strong{Spatial:} NNI drives relative deficit weights (lower NNI, higher N share).
\item \strong{Economic:} \code{\link[Rquefts:optApp]{Rquefts::optApp()}} per cell on QUEFTS with soil N supply
from balance component B.
\item \strong{Operational:} \code{\link[Rquefts:fertApp]{Rquefts::fertApp()}} for a cost-optimal mix (optional;
requires valid \code{price_kg} and compatible fertilizer table).
}

Requires suggested packages \strong{Rquefts}, \strong{terra}, and \strong{limSolve}
(via Rquefts for LP).
}
\examples{
\dontrun{
fld <- list(
  crop = "Grano duro (granella)",
  expected_yield_tons_ha = 6,
  ccp = "Spring-summer crop 100-130 days",
  sand = 15.5, clay = 18.5, Ntot = 1.5, SOM = 2.0, CN = 7.73,
  oxygen_availability = "Slow",
  winter_rain = 150, start_spring_rain = 0,
  prev_crop = "Winter cereals straw removal",
  source = "Cattle slurry", fertorg_frequency = "every year",
  location = "Isolated plain",
  forg_quantity = 0, organic_previous_year_N = 0, E_to_D = TRUE
)
r <- terra::rast(nrows = 5, ncols = 5, xmin = 0, xmax = 50, ymin = 0, ymax = 50,
                 crs = "EPSG:32632", vals = runif(25, 0.7, 1.05))
out <- optimize_VR_DPI(field_inputs = fld, NNI_raster = r, prices = list(p_y = 280))
terra::plot(out$N_prescription)
}

}
