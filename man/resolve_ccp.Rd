% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_lookup.R
\name{resolve_ccp}
\alias{resolve_ccp}
\title{Resolve a \code{ccp} (crop calendar period) string with whitespace and dash
normalisation}
\usage{
resolve_ccp(x, table = NFert::coef_time)
}
\arguments{
\item{x}{Character. The \code{ccp} string to resolve.}

\item{table}{Lookup with column \code{ccp}; default \code{NFert::coef_time}.}
}
\value{
The canonical \code{ccp} string (as in \code{table$ccp}), or \code{x} unchanged
with a warning if not found.
}
\description{
Handles common variations between user input and the canonical English
value stored in \code{coef_time$ccp}:
\itemize{
\item Trims outer whitespace and collapses internal multiple spaces.
\item Replaces en-dash (U+2013) and em-dash (U+2014) with hyphen.
\item Case-insensitive match.
}
}
\examples{
resolve_ccp("Spring-summer crop 100\u2013130 days")  # em-dash
resolve_ccp("autumn-winter crop <150 days")         # case
}
