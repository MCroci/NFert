% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fert_plan.R
\name{plan_distribution}
\alias{plan_distribution}
\title{Fertilization distribution plan}
\usage{
plan_distribution(
  soil_group,
  n_balance = NULL,
  p_balance = NULL,
  k_balance = NULL,
  organic_rows = list(),
  mineral_rows = list(),
  zvn = FALSE,
  organic_fertilizers.table = nfert_data_get("organic_fertilizers.table"),
  mineral_fertilizers.table = nfert_data_get("mineral_fertilizers.table"),
  efficiency.table = nfert_data_get("efficiency.table"),
  distribution_modalities.table = nfert_data_get("distribution_modalities.table"),
  texture_groups.table = nfert_data_get("texture_groups.table")
)
}
\arguments{
\item{soil_group}{Italian DPI texture group.}

\item{n_balance, p_balance, k_balance}{Optional numeric requirements (kg/ha).}

\item{organic_rows}{A list of lists, each with:
\itemize{
\item \code{fertilizer}: name matching \code{organic_fertilizers.table$fertilizer} (or a custom
name plus explicit titres)
\item \code{quantity_t_ha}: amount in t/ha (fresh matter)
\item \code{year}: application year (e.g. 2024, 2025)
\item \code{modality_epoch}: ID or label from \code{distribution_modalities.table}
\item \code{level} (optional): efficiency level \code{"bassa"/"media"/"alta"} if you
want to override the default selection.
\item Optional \code{N_pct}, \code{P2O5_pct}, \code{K2O_pct}, \code{dry_matter_pct} override the table.
}}

\item{mineral_rows}{A list of lists, each with:
\itemize{
\item \code{fertilizer}: matching \code{mineral_fertilizers.table$fertilizer}, or custom
\item \code{quantity_q_ha}: quintals/ha (100 kg/ha)
\item \code{modality_epoch}: ID or label from \code{distribution_modalities.table}
\item \code{N}, \code{P2O5}, \code{K2O} titres (kg per q) if custom.
}}

\item{zvn}{Logical, if TRUE, enforce the ZVN 170 kg N/ha zootec cap.}

\item{organic_fertilizers.table, mineral_fertilizers.table, efficiency.table, distribution_modalities.table, texture_groups.table}{Defaults.}
}
\value{
A list with:
\itemize{
\item \code{rows}: per-application data frame (fertilizer, quantity, delivered N/P/K, efficiency, useful N/P/K)
\item \code{totals}: totals of useful N, P2O5, K2O delivered
\item \code{alerts}: if balances provided, OK/Eccesso/Deficit for each nutrient
\item \code{zvn_zoot_N}: total zootec N delivered (useful for ZVN check)
}
}
\description{
Builds the fertilisation distribution plan for a crop according to DPI
Emilia-Romagna 2026 (Fert_Office v1.26 foglio \code{Distribuz}). Combines:
}
\details{
\itemize{
\item Organic applications (one or more matrices from \code{organic_fertilizers.table}, with
efficiency selected from \code{efficiency.table} by soil group, dose level,
and sector of matrix).
\item Mineral / synthesis fertilisers (from \code{mineral_fertilizers.table} or user-specified
triplets of N, P2O5, K2O titres).
}

For each input row the function computes the "useful" contribution to the
crop for N (via efficiency), P2O5 and K2O (assumed 100\%), and aggregates
the totals. If \code{n_balance}, \code{p_balance}, \code{k_balance} are provided the
function also checks the totals against the required doses and returns
alerts (\code{OK} / \code{Eccesso} / \code{Deficit}).
}
