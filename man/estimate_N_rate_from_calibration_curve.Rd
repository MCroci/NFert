% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_N_rate_from_calibration_curve.R
\name{estimate_N_rate_from_calibration_curve}
\alias{estimate_N_rate_from_calibration_curve}
\title{Nitrogen Rate Estimation from NDVI using Calibration}
\usage{
estimate_N_rate_from_calibration_curve(
  raster,
  minN,
  maxN,
  meanN = NULL,
  calibration_type = "two-point",
  plot = FALSE
)
}
\arguments{
\item{raster}{A \code{raster::RasterLayer} with NDVI values, or a \code{RasterBrick} /
\code{RasterStack} (the layer named \code{NDVI} is used if present, otherwise the first layer).}

\item{minN}{The minimum N rate (kg/ha) corresponding to the minimum NDVI.}

\item{maxN}{The maximum N rate (kg/ha) corresponding to the maximum NDVI.}

\item{meanN}{The mean N rate (kg/ha) corresponding to the mean NDVI (only used for three-point calibration).}

\item{calibration_type}{The type of calibration to perform: "two-point" or "three-point". Default is "two-point".}

\item{plot}{A logical value indicating whether to create plots (default is FALSE).}
}
\value{
A RasterLayer object with the estimated N rates based on the chosen calibration method.
}
\description{
This function estimates nitrogen (N) application rates based on NDVI values using either
a two-point or three-point calibration method.
}
\examples{
# Load example NDVI raster (replace with your own)
# ndvi_raster <- raster::raster(system.file("extdata/s2.tif", package = "NFert"))

# Two-point calibration
# n_rate_raster_2pt <- estimate_N_rate_from_calibration_curve(ndvi_raster, minN = 40, maxN = 60)

# Three-point calibration
# n_rate_raster_3pt <- estimate_N_rate_from_calibration_curve(ndvi_raster, minN = 40, meanN = 50,
# maxN = 60, calibration_type = "three-point")
}
