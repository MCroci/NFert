# Map legacy oxygen / drainage labels to `ca.table$availability` (Slow / Normal / Fast).

Map legacy oxygen / drainage labels to `ca.table$availability` (Slow /
Normal / Fast).

## Usage

``` r
nfert_normalize_oxygen(x)
```
